---
import { getCollection } from "astro:content";
import HorizontalCard from "../components/HorizontalCard.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import createSlug from "../lib/createSlug";

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hey there 👋</div>
    <div class="text-5xl font-bold">I'm Mahyar Jahani-nasab</div>
    <div class="py-2">
      <text class="text-lg">
        Recognized (to my surprise) by peers for having a knack for research, I’m happiest when I’ve thrown myself into a brand-new scientific rabbit hole—somehow managing to learn the latest advances fast enough to come up with fresh ideas before I get completely lost. My real curiosity lies where neuro-symbolic, generative AI, physics-informed machine learning, and heavy mathematical frameworks all meet—basically, the nerd’s Bermuda Triangle. 
        I balance theory with practice: I can wrangle hundreds of lines of code, build sturdy software pipelines, and occasionally forget what day it is while polishing a solution until it actually looks elegant.
      </text>
    </div>
    <div class="mt-8">
      <a class="btn" href="https://www.linkedin.com/in/mahyar-jahaninasab-050342115" target="_blank"> Let's connect!</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">Recent Research Endeavors {"</>"}</div>
  </div>

  <HorizontalCard
    title="Assured Neuro-symbolic Sequential Decision Making with Physics Informed Neural Networks"
    img="/lightbulb.webp"
    desc="Working on making new AI systems that combine thinking in clear steps with learning from real-world physics using physics informed machine learning and neuro-symbolic."
    url="#"
    badge="Ph.D."
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Redesigning RNNs for Fully Physics-Informed Loss Function"
    img="/MIRNN.webp"
    desc="Training Recurrent Neural Networks (RNNs) solely through physics-informed loss functions presents significant challenges,  necessitating numerical derivatives between blocks. In this work, we introduce structural modifications to RNNs that eliminate the dependency on numerical derivatives, allowing for the formation of purely physics-informed loss functions using backpropagation. The archived version of this paper is currently accessible, with the source code set for release upon official publication."
    url="https://link.springer.com/article/10.1007/s00466-025-02691-5"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Enhancing Convergence Speed of PINN"
    img="/sakhtar.webp"
    desc="Streamline your training with PINNs! Dive into our newly published paper and access the source code to double the convergence speed and boost the success rate of your Physics-Informed Neural Networks."
    url="https://github.com/mahyar-jahaninasab/Feature-Enforcing-PINN"
    badge="OSS"
  />

  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + createSlug(post.data.title, post.slug)}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
